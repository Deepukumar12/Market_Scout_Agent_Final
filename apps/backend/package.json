{
  "name": "@market-scout/backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "source venv/bin/activate && PYTHONPATH=\"../../\" uvicorn src.main:app --reload --port 8000",
    "start": "source venv/bin/activate && PYTHONPATH=\"../../\" uvicorn src.main:app --reload --port 8000",
    "build": "echo 'No build step required for Python backend'",
    "setup": "python3 -m venv venv && source venv/bin/activate && pip install -r requirements.txt"
  }
}
