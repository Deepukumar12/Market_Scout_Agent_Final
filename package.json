{
  "name": "market-scout-agent",
  "version": "1.0.0",
  "description": "Market Scout Agent Platform",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "start": "lsof -t -i :8000 | xargs kill -9 2>/dev/null || true; cd backend && source venv/bin/activate && uvicorn app.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "author": "",
  "license": "ISC"
}
